import { NAME } from "app-src";
import { greet } from "../app-src/utils";
import Button from "../components/button";
import * as components from "../components";
console.log("greet %s", greet, NAME, Button, components);